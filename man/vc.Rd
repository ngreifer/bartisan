% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/varying.R
\name{vc}
\alias{vc}
\title{Give a predictor a varying coefficient}
\usage{
vc(x, modifiers = NULL, center = "auto")
}
\arguments{
\item{x}{the predictor whose coefficient varies, named as a bare variable
rather than as an expression. A numeric variable gets one forest; a factor
gets one per level, coded symmetrically (i.e., with no level held out as a
reference) as \code{\link[=multinomial]{multinomial()}} codes its predictors.}

\item{modifiers}{a one-sided formula naming the variables this coefficient's
forest may split on. Default is \code{NULL} to allow every predictor in the model
except \code{x} itself. A variable the model formula's fixed part does not carry
may be named here, and it then modifies the coefficient without entering
the control function; see Details. Naming something that is not a column of
\code{data} at all is an error rather than a silent restriction.}

\item{center}{the value of \code{x} at which the control function is read, given as
either a string or a number. Allowable options include \code{"auto"} (the
default), \code{"mean"}, \code{"zero"}, \code{"mid"}, and \code{"estimate"}. \code{"mean"} centers
\code{x}, \code{"zero"} leaves it alone, \code{"mid"} uses the midpoint of its range, a
number uses that number, and \code{"auto"} picks between \code{"zero"} and \code{"mean"} by
the covariate. For a factor, \code{center} is \code{"mean"} or the name of a level to
report against. \code{"estimate"} draws the coding rather than fixing it, and
needs a covariate with between two and twenty distinct values. See Details.}
}
\value{
Nothing; \code{vc()} is never evaluated, and calling it directly is an error.
}
\description{
Marks a predictor inside a \code{\link[=bartisan]{bartisan()}} formula as one whose coefficient is a
function of the other predictors rather than a constant, so that the
coefficient gets a forest and a prior of its own. \code{vc()} is a marker rather
than a function: it is read out of the formula it appears in and throws an
error if it is called on its own.
}
\details{
The model is

\deqn{g(\mu_i) = f_0(Z_i) + \sum_j (X_{ij} - c_j) f_j(Z_i)}

with a forest for the control function \eqn{f_0} and one for each varying
coefficient \eqn{f_j}. Every forest is fitted at once, so the coefficient has
a prior of its own rather than being whatever difference a single forest with
\code{x} among its predictors happens to produce.
\subsection{Setting \code{modifiers}}{

By default a coefficient may vary with every predictor in the model except
\code{x} itself, and \code{modifiers} narrows that.

It can also widen it. A variable named here that the fixed part leaves out is
given a column of its own and reaches this coefficient's forest and no other,
so the coefficient varies with it while the control function stays blind to
it. That asymmetry is the point rather than a side effect: it is how an
effect is allowed to depend on something a prognostic function must not see.
The case that needs it is a modifier from which the treatment could be
reconstructed: a control function allowed to see such a variable can
represent the treatment itself, so the effect is not identified, even though
the effect may legitimately vary with it. Naming it as a modifier alone keeps
it out of the control function. Only an explicit \code{modifiers} formula reaches
such a variable: a bare \code{vc(z)} means the predictors of the model, which
these are not.

A covariate with a varying coefficient is kept out of the control function,
since \eqn{f_0(Z) + z f_1(Z)} is not identified when \code{z} is among
\eqn{f_0}'s predictors: any function of \code{z} moves between the two. Reached
through \code{.} it is dropped quietly, because \code{.} did not name it; named
outright, the model is fitted as asked and a warning says why that is a
choice.

A numeric covariate may modify its own coefficient, and that is how the effect
stops being linear in it. \code{vc(z, ~ z + x1)} fits \eqn{z f_1(z, x_1)}, so the
slope moves across \code{z}'s range and the dose response is a curve rather than a
line, which is worth reaching for whenever the effect of a continuous
predictor might not be proportional to it. A categorical covariate is removed
from its own forests instead: a level's indicator is nonzero only on the rows
where that level holds, and the variable is constant on exactly those rows, so
such a split separates rows that contribute from rows that contribute nothing.

At the other extreme, \code{~ 1} names nothing at all, which leaves the
coefficient's forest no predictor to split on: every tree in it is a stump,
so the coefficient is one drawn number and \code{x} enters as a linear term while
the rest of the model stays nonparametric. Comparing \code{vc(z, ~ 1)} against
\code{vc(z)} with \link[=bartisan-interop]{loo()} is then a test of whether the effect
of \code{z} varies at all, and the constant fit reports it as a single
coefficient, which under a logit link is one conditional log odds ratio. It
is drawn under the leaf prior rather than a prior written for a regression
coefficient, so it is shrunk toward zero. See
\code{vignette("comparison", package = "bartisan")}.
}

\subsection{Setting \code{center}}{

Centering is a reparameterization of \eqn{f_0} alone: every coefficient and
every estimand is identical under any choice, and what changes is what the
control function means. \code{"auto"} picks by the covariate. For a \code{0}/\code{1}
covariate it uses zero, so \eqn{f_0} is the surface among the untreated,
which is a quantity with its own meaning. For any other numeric covariate it
uses the mean, because zero may be nowhere near the data and the control
function there would be an extrapolation.

A factor is always fitted mean-centered and gets one forest per level, coded
symmetrically rather than as contrasts against a level that happened to sort
first. That coding leaves the reference a reporting choice: \code{center} names the
level \code{\link[=coef]{coef()}} reports against, and no refit is needed to change it.
}

\subsection{Drawing the Coding (\code{center = "estimate"})}{

This is different in kind from the choices above. Rather than subtract a
number from \code{x}, it gives each of \code{x}'s values a coefficient of its own and
draws it, so that every contrast carries the same prior whatever the number of
values and no value is a reference. \code{\link[=coef]{coef()}} returns the identified contrasts.

At two values it restricts nothing, and it is what \code{\link[=bcf]{bcf()}} uses: what it buys
is that the answer stops depending on which level was written as 1. Above two
values every contrast becomes one shared shape times a scalar, where the
symmetric coding gives each level its own, so it is the parsimonious model
against a general one. Reach for it when the levels plausibly differ in degree
rather than in kind.
}

\subsection{Families with Several Additive Predictors}{

\code{gaussian_ls()}, \code{zi_poisson()} and the rest fit one forest per
distributional parameter, and each parameter's formula carries its own \code{vc()}
terms. The forests are then two-dimensional (a control function and its
coefficients, for each parameter) and named accordingly, which is what
per-forest settings are keyed by:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# forests: mean, mean:z, log_sd
bartisan(list(mean = y ~ x1 + x2 + vc(z), log_sd = ~ x1 + x2), data = d,
         family = gaussian_ls())

# forests: mean, mean:z, log_sd, log_sd:z; one formula reaches every
# parameter, which is the rule every per-forest argument follows
bartisan(y ~ x1 + x2 + vc(z), data = d, family = gaussian_ls())

# each coefficient with its own modifiers
bartisan(list(mean = y ~ x1 + x2 + vc(z, ~ x2),
              log_sd = ~ x1 + x2 + vc(z, ~ x1)), data = d,
         family = gaussian_ls())
}\if{html}{\out{</div>}}

So the same covariate may have a coefficient on more than one parameter:
\eqn{z} shifting the mean and widening the spread are different questions, and
both are answered at once. \code{\link[=coef]{coef()}} returns one column per coefficient, named
for its forest.

Two things follow from the parameters being different. A group intercept from
\code{(1 | g)} reaches every control function and no coefficient, since a
group-varying coefficient is a random slope. And \code{center = "estimate"} is
judged per parameter: the drawn coding needs a leaf target that is quadratic
in the predictor it feeds, which \code{gaussian_ls()} is in the mean and is not
in the log standard deviation, so the same request is accepted on one and
refused on the other.

The two multinomial families are the exception and refuse \code{vc()}. Their forests
are the levels of one parameter rather than separate parameters, identified
only up to a function they all share, and reporting removes it; a coefficient
forest per level would add one such direction per coefficient and the
reporting does not carry them.
}
}
\examples{
data("rhc")

set.seed(123)

# The effect of right heart catheterization on death, free to vary with
# every other covariate. `rhc` reaches the fixed part through `.`, so it is
# dropped from the control function, which is what keeps the two identified
fit <- bartisan(death ~ . - days + vc(rhc), data = rhc,
                family = binomial(), num_trees = 10, num_burn = 50,
                num_draws = 50)

# One coefficient per patient, which is what a coefficient function comes to
head(coef(fit))

# The same effect, free to vary with severity of illness alone
fit2 <- bartisan(death ~ . - days + vc(rhc, ~ aps), data = rhc,
                 family = binomial(), num_trees = 10, num_burn = 50,
                 num_draws = 50)

head(coef(fit2))

}
\seealso{
\itemize{
\item \code{\link[=bartisan]{bartisan()}} for the formula interface
\item \code{\link[=bcf]{bcf()}} for the causal case, which is this term with the priors and the
propensity score set up for it
\item \code{\link[=coef.bartisan_fit]{coef.bartisan_fit()}} for reading the coefficients out
\item \code{\link{bartisan-families}} for the order the forests come in
}
}
