% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{.bartisan_predict}
\alias{.bartisan_predict}
\title{Evaluate stored forests at new data}
\usage{
.bartisan_predict(
  X,
  forest_flat,
  tree_start,
  bandwidth,
  num_forest,
  num_trees,
  num_draws,
  soft,
  gate,
  iterations,
  codes,
  tree_mask
)
}
\arguments{
\item{X}{a design matrix with entries in \verb{[0, 1]}.}

\item{forest_flat, tree_start}{the encoded forests returned by
\code{.bartisan_fit()}.}

\item{bandwidth}{a matrix of per-tree bandwidths.}

\item{num_forest, num_trees, num_draws}{\code{integer}; the dimensions of the
stored chain.}

\item{soft}{\code{logical}; whether the decision rules are soft.}

\item{gate}{\code{integer}; which gate the soft rules use; see \code{GateShape} in
\code{node.h}.}

\item{iterations}{\code{integer}; the zero-based saved iterations to evaluate.}

\item{codes}{a matrix of level codes for the categorical rules.}

\item{tree_mask}{\code{logical}; one entry per stored tree, in the order
\code{.bartisan_tree_uses()} reports, saying whether to evaluate it. A
zero-length vector evaluates every tree, which is the ordinary case; a
subset is what partial dependence uses to avoid re-evaluating the trees
that cannot move across its grid.}
}
\value{
A list of \code{num_forest} matrices of additive predictors.
}
\description{
Evaluate stored forests at new data
}
\keyword{internal}
