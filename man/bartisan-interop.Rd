% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop.R
\name{bartisan-interop}
\alias{bartisan-interop}
\alias{posterior_predict.bartisan_fit}
\alias{posterior_epred.bartisan_fit}
\alias{posterior_linpred.bartisan_fit}
\alias{log_lik.bartisan_fit}
\alias{simulate.bartisan_fit}
\alias{fitted.bartisan_fit}
\alias{residuals.bartisan_fit}
\alias{weights.bartisan_fit}
\alias{sigma.bartisan_fit}
\alias{prior_summary.bartisan_fit}
\alias{print.bartisan_prior_summary}
\alias{loo.bartisan_fit}
\alias{waic.bartisan_fit}
\alias{kfold.bartisan_fit}
\alias{pp_check.bartisan_fit}
\alias{as_draws.bartisan_fit}
\alias{r2_posterior.bartisan_fit}
\alias{r2.bartisan_fit}
\alias{model_performance.bartisan_fit}
\title{Interfaces to other packages}
\usage{
\method{posterior_predict}{bartisan_fit}(
  object,
  newdata = NULL,
  iterations = NULL,
  offset = NULL,
  weights = NULL,
  ...
)

\method{posterior_epred}{bartisan_fit}(object, newdata = NULL, ...)

\method{posterior_linpred}{bartisan_fit}(object, transform = FALSE, newdata = NULL, ...)

\method{log_lik}{bartisan_fit}(object, newdata = NULL, ...)

\method{simulate}{bartisan_fit}(object, nsim = 1, seed = NULL, ...)

\method{fitted}{bartisan_fit}(object, type = "response", ...)

\method{residuals}{bartisan_fit}(object, ...)

\method{weights}{bartisan_fit}(object, ...)

\method{sigma}{bartisan_fit}(object, ...)

\method{prior_summary}{bartisan_fit}(object, ...)

\method{print}{bartisan_prior_summary}(x, digits = 3L, ...)

\method{loo}{bartisan_fit}(x, scale = NULL, ...)

\method{waic}{bartisan_fit}(x, scale = NULL, ...)

\method{kfold}{bartisan_fit}(x, K = 10, folds = NULL, scale = NULL, save_fits = FALSE, ...)

\method{pp_check}{bartisan_fit}(object, type = "dens_overlay", ndraws = 10, ...)

\method{as_draws}{bartisan_fit}(x, eta = TRUE, ...)

\method{r2_posterior}{bartisan_fit}(model, verbose = TRUE, ...)

\method{r2}{bartisan_fit}(model, ...)

\method{model_performance}{bartisan_fit}(model, metrics = "all", verbose = TRUE, ...)
}
\arguments{
\item{object, model, x}{a \verb{<bartisan_fit>} object; the output of a call to
\code{\link[=bartisan]{bartisan()}}.}

\item{newdata}{optional; a data frame at which to predict. Default is \code{NULL}
to use the data the model was fit to. Missing predictor values are allowed
in the columns that had them when the model was fit, since only those
columns' splitting rules carry an answer for one; a missing value anywhere
else is an error. See section \emph{Missing Predictor Values} in the Details of
\code{\link[=bartisan]{bartisan()}}.}

\item{iterations}{\code{numeric}; optional indices of the stored draws to use,
between 1 and the number of draws the fit retains. Default is \code{NULL} to use
all of them.}

\item{offset, weights}{an offset and prior weights for \code{newdata}, as in
\code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}}. Defaults are \code{NULL} to use those the model was fit
with. For a binomial response the weights are the numbers of trials, and so
give the denominator of a replicate outcome; they must be given alongside
\code{newdata} when the model was fit with more than one trial, since the number
of trials is not a function of the predictors and cannot be reconstructed.}

\item{...}{further arguments, passed to whatever the method calls (the
\pkg{bayesplot} check from \code{pp_check()}, \pkgfun{loo}{loo} and
\pkgfun{loo}{waic} from \code{loo()} and \code{waic()}, and
\code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}} from the rest) and ignored where there is nowhere
to pass them.}

\item{transform}{\code{logical}; for \code{posterior_linpred()}, whether to map the
predictor through the inverse link, as \code{posterior_epred()} does.
Default is \code{FALSE}.}

\item{nsim, ndraws}{\code{numeric}; the number of posterior draws to use, chosen at
random from the retained ones. Defaults are 1 for \code{simulate()} and 10 for
\code{pp_check()}. A \verb{ppc_loo_*} check uses every retained draw whatever this is
set to, and says so; see Details.}

\item{seed}{optional seed, set with \code{\link[=set.seed]{set.seed()}} before drawing and restored
afterwards, following the \code{\link[stats:simulate]{stats::simulate()}} convention. Default is \code{NULL}
to leave the stream alone.}

\item{type}{string; for \code{fitted()}, the prediction scale, passed to
\code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}}; default is \code{"response"}. For \code{pp_check()}, the
name of the \pkg{bayesplot} check to run without its \code{ppc_} prefix, so that
\code{"dens_overlay"} (the default) calls
\pkgfun{bayesplot}{ppc_dens_overlay}; \pkgfun{bayesplot}{available_ppc}
lists them.}

\item{digits}{\code{integer}; for \code{print()} on the output of \code{prior_summary()},
how many digits to round the prior's scales to. Default is 3.}

\item{scale}{\code{string}; for \code{loo()}, \code{waic()} and \code{kfold()} on a survival fit,
the
measure to report the pointwise densities with respect to: \code{"time"} for the
density of \eqn{T} and \code{"log_time"} for the density of \eqn{\log T}.
Default is \code{NULL} to use the family's own, which is \eqn{\log T} for the
accelerated failure time families and \eqn{T} for \code{\link[=ph]{ph()}}. A fit already on
the scale named is left alone, so naming one scale for every model in a
comparison is enough. See Details.}

\item{K}{\code{numeric}; for \code{kfold()}, how many folds to split the sample into.
Default is 10. Ignored when \code{folds} is given.}

\item{folds}{optional; for \code{kfold()}, an integer vector of one fold number
per observation, as \pkgfun{loo}{kfold_split_random} and its relatives
return. Default is \code{NULL} to draw them at random. Supply them to stratify,
to group, or to score two models on the same split.}

\item{save_fits}{\code{logical}; for \code{kfold()}, whether to keep the \eqn{K} refits
in the result's \code{fits} element. Default is \code{FALSE}, since each is a whole
fit.}

\item{eta}{for \code{as_draws()}, which columns of the additive predictor to carry
into the draws array alongside the scalar parameters, given as either a
logical value or a numeric vector. Default is \code{TRUE}, which takes a
representative ten spread across the range of the fitted function; \code{FALSE}
takes none, and a numeric vector takes those observations. The default takes
a handful rather than all of them because there is one column per
observation, and an array with thousands of them is not something
\pkgfun{posterior}{summarise_draws} or a trace plot can be pointed at. The
predictor is the quantity whose convergence usually matters, and the one
\code{\link[=diagnose]{diagnose()}} reports on, so it is included by default.}

\item{verbose}{\code{logical}; whether to report problems that do not stop the
computation, such as a family that has no mean and so no Bayesian
\eqn{R^2}. Default is \code{TRUE}.}

\item{metrics}{\code{character}; for \code{model_performance()}, which fit statistics to
report. Allowable options include \code{"all"} (the default), \code{"ELPD"},
\code{"LOOIC"}, \code{"WAIC"}, \code{"R2"}, \code{"RMSE"}, and \code{"SIGMA"}, and a vector of them
selects several.}
}
\value{
\code{kfold()} returns a \verb{<kfold>} object, a list whose \code{estimates} holds
\code{elpd_kfold}, \code{p_kfold} and \code{kfoldic} with their standard errors, whose
\code{pointwise} holds the same three per observation, and whose \code{folds} records
the split; setting \code{save_fits = TRUE} adds the \eqn{K} refits in \code{fits}.

\code{posterior_predict()}, \code{posterior_epred()}, \code{posterior_linpred()} and
\code{log_lik()} return a matrix of draws by observations. \code{simulate()} returns a
data frame of one column per replicate. \code{loo()} and \code{waic()} return the
\verb{<loo>} and \verb{<waic>} objects those functions produce, and
\code{model_performance()} a one-row data frame of class \verb{<performance_model>}.
\code{as_draws()} returns a \verb{<draws_array>} of iterations by chains by parameters.

\code{prior_summary()} returns a \verb{<bartisan_prior_summary>} object, a list whose
\code{forests} is a data frame of one row per additive predictor and one column per
setting the prior is made of, whose \code{estimated} says in the same shape which
of them were drawn rather than held, and whose \code{family} holds the family's own
parameters with the prior each was given. \code{random} and \code{response} record the
group-intercept scale and what was read off the response.

The accessors return what their names suggest.
}
\description{
Methods that let a \verb{<bartisan_fit>} object be used by the packages that assess
model fit. There is nothing to set up: load the other package and call its
function on the fit.
}
\details{
\subsection{Available Methods}{

\pkgfun{rstantools}{posterior_predict} draws replicate outcomes from the
fitted model, \pkgfun{rstantools}{posterior_epred} gives their mean and
\pkgfun{rstantools}{posterior_linpred} the additive predictor, following the
\pkg{rstantools} conventions that \pkg{brms} and \pkg{rstanarm} follow, and
\code{\link[stats:simulate]{stats::simulate()}} is the same thing in the shape base R expects.
\pkgfun{rstantools}{log_lik} returns the draws-by-observations matrix of
log-likelihood contributions, which \pkgfun{loo}{loo} and
\pkgfun{loo}{waic} need.

\code{pp_check()} runs any of the \pkg{bayesplot} posterior-predictive checks on
the fit, \pkgfun{performance}{model_performance} collects the fit statistics
in one table, \pkgfun{performance}{r2} gives the Bayesian \eqn{R^2}, and
\pkgfun{posterior}{as_draws} hands the scalar parameters to
\pkgfun{posterior}{summarise_draws} or to the \pkg{bayesplot} MCMC
diagnostics. \pkgfun{rstantools}{prior_summary} writes out every prior the fit
was given, on the scale it was given on, which is the companion to
\code{prior_only = TRUE} in \code{\link[=bartisan]{bartisan()}}: one says what the prior is and the other
what it implies about the outcome. \code{\link[stats:fitted]{stats::fitted()}}, \code{\link[stats:residuals]{stats::residuals()}},
\code{\link[stats:weights]{stats::weights()}} and \code{\link[stats:sigma]{stats::sigma()}} do what they do for a \code{glm}, which is
also most of what \pkg{insight} needs to make the fit legible to the
\pkg{easystats} packages.
}

\subsection{Accuracy of the Leave-One-Out Approximation}{

\pkgfun{loo}{loo} estimates the leave-one-out predictive density by importance
sampling from the full-data posterior, and the estimate is trustworthy only
when the importance weights have a finite variance, which the Pareto
\eqn{k} diagnostic reports on. A forest is a flexible function of the
predictors, so the worry is that one observation carries enough influence over
the leaves it lands in that dropping it cannot be approximated from the fit in
hand. In practice it rarely does: the leaf prior shrinks every leaf toward zero
and the fit is a sum over many trees, so no single observation dominates.

The warning \pkg{loo} prints there is therefore worth reading rather than
expecting, and the exceptions that do turn up are usually about the likelihood
rather than the trees. When it names a handful of observations, those are the
influential ones, and refitting without them shows how badly they are
predicted. A log score on data the model has not seen is available directly:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{predict(fit, newdata = held_out, type = "density", log = TRUE)
}\if{html}{\out{</div>}}
}

\subsection{Cross-Validation (\code{kfold()})}{

Where \code{loo()} estimates the leave-one-out density from one fit, \code{kfold()}
splits the sample, refits \eqn{K} times, and scores each part under a fit that
never saw it. That costs \eqn{K} fits and owes nothing to an approximation,
which makes it the thing to reach for when the Pareto diagnostics say the
weights cannot be trusted.

It returns a \verb{<kfold>} object that \pkgfun{loo}{loo_compare} accepts beside a
\verb{<loo>} one, so two models can be compared on one split by passing the folds
from the first to the second:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{folds <- loo::kfold_split_random(K = 10, N = nobs(fit))

loo_compare(list(full = kfold(fit, folds = folds),
                 small = kfold(other, folds = folds)))
}\if{html}{\out{</div>}}

The refits run under a \code{future} plan when one is set, and one \code{set.seed()}
reproduces them either way. Each is refitted from the original call, so the
\code{data} argument has to still name the data the fit was made from. Prior weights
and an offset are carried into both the refits and the held-out scores.
\code{p_kfold} is the gap between what the model predicts for an observation it was
fitted to and what it predicts for the same one held out, which is the price of
having used it. \code{vignette("comparison")} reads an example.
}

\subsection{Setting \code{scale} for Survival Families}{

The accelerated failure time families report the density of \eqn{\log T} and
\code{\link[=ph]{ph()}} the density of \eqn{T}. Both are correct for the model that produced
them, and they differ by the Jacobian of the change of variable, so a log score
taken across that boundary is off by \eqn{\sum \log t} over the events, which
can reverse which family looks better.

The \code{scale} argument puts them on one measure, and reads the same from either side, since a
fit already on the scale named is returned untouched:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{loo_compare(list(aft = loo(aft_fit, scale = "time"),
                 ph = loo(ph_fit, scale = "time")))
}\if{html}{\out{</div>}}

It is left to the caller rather than applied automatically so that \code{loo()}
keeps reporting the model's own predictive density and keeps agreeing with
\code{log_lik()}. Censored observations are not adjusted, a survival probability
being a probability on either scale. \code{vignette("comparison")} works through the
comparison and \code{vignette("survival")} through the families.
}

\subsection{Posterior Predictive Checks}{

The seven \verb{ppc_loo_*} checks reweight the replicates towards the
leave-one-out predictive, so they need those weights. \code{pp_check()} computes
them from the fit's own pointwise log likelihood and passes them on, and
the \code{ndraws} argument does not apply to those checks, because the weights and the
replicates have to line up draw for draw; supplying \code{lw} or \code{psis_object}
takes over from it.

The two calibration checks are the ones to reach for when the response is
binary, since the default check compares two distributions that can only take
two values. Setting \code{type = "loo_calibration"} gives the honest one, holding each
observation out of the probability it is judged against, where
\code{type = "calibration"} is its in-sample counterpart and reads optimistically.
Those two and a binned residual plot (\code{type = "error_binned"}) are about the
predicted probabilities rather than replicate outcomes, so they are passed the
mean of the predictive distribution instead of a draw from it.
}

\subsection{The Scale of a Posterior Predictive Draw}{

Replicate outcomes are on the scale the likelihood was written on, which is
the scale \code{\link[=bartisan]{bartisan()}} stored the response on. A binomial response is a
proportion, so binary data come back as 0 and 1, and data given as two columns
or with prior weights come back as a fraction of the trials. A response with
categories comes back as an integer category index, since a matrix cannot hold
a factor; \code{fit$levels} names them, and \code{\link[stats:simulate]{stats::simulate()}} returns factors
instead, its result being a data frame. An accelerated failure time response
comes back as an event time rather than a log time, and the predictive
distribution of the outcome knows nothing of the censoring that may have hidden
it, so \code{pp_check()} says so when replicates are compared against censored
observations. A \code{\link[=custom_family]{custom_family()}} fit supplies a log density and no way to draw
from it, so these methods error on one.
}

\subsection{AIC, BIC and Normality Checks}{

\code{\link[stats:AIC]{stats::AIC()}} and \code{\link[stats:BIC]{stats::BIC()}} need a count of parameters, which a forest
does not have, since the number of leaves is itself drawn from the posterior;
there is accordingly no \code{logLik()} method. \pkgfun{loo}{loo} and
\pkgfun{loo}{waic} are the corresponding quantities for a model like this one,
computed from the posterior rather than from a parameter count. For the same
reason \pkgfun{performance}{check_normality} and
\pkgfun{performance}{check_outliers}, which ask for a likelihood-ratio test
and for Cook's distance, are unavailable, where
\pkgfun{performance}{check_predictions} works through \code{\link[stats:simulate]{stats::simulate()}}.
}

\subsection{The Bayesian R-Squared}{

\pkgfun{performance}{r2} returns, per draw, the variance of the fitted means
across observations divided by that variance plus the variance of the
residuals. Being a per-draw quantity it has a posterior, which is why it is
reported with an interval and why it can fall as the model is made more
flexible. It needs a mean, so it is available for every family except
\code{ordinal()} and \code{multinomial()}.
}
}
\examples{
\dontshow{if (rlang::is_installed(c("loo", "rstantools"))) withAutoprint(\{ # examplesIf}
data("rhc")
set.seed(123)

fit <- bartisan(death ~ . - days, data = rhc, num_trees = 10,
                num_burn = 50, num_draws = 50, chains = 2, verbose = FALSE)

# Replicate outcomes, one per draw per observation, whose mean
# `fitted()` reports
yrep <- rstantools::posterior_predict(fit)
range(colMeans(rstantools::posterior_epred(fit)) - fitted(fit))

# Pointwise log likelihood, and the fit statistics built on it
loo::waic(rstantools::log_lik(fit))

# Every prior the fit was given, on the scale it was given on
rstantools::prior_summary(fit)

# Whether replicate outcomes look like the observed ones
if (rlang::is_installed("bayesplot")) {
  bayesplot::pp_check(fit, type = "bars")
}

# The scalar parameters and a spread of the predictor, as a draws array
if (rlang::is_installed("posterior")) {
  posterior::summarise_draws(posterior::as_draws(fit))
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}} for the predictions these methods are built on;
\code{\link[=diagnose]{diagnose()}} for the convergence and mixing diagnostics;
\code{\link{bartisan-marginaleffects}} for reading effects off a fit;
\code{vignette("diagnostics")} for the fuller treatment
}
