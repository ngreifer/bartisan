% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{genbart-families}
\alias{genbart-families}
\alias{negbin}
\alias{Gamma_shape}
\alias{ordinal}
\alias{multinomial}
\alias{weibull_aft}
\alias{loglogistic_aft}
\alias{lognormal_aft}
\alias{location_scale}
\alias{zi_poisson}
\alias{zi_negbin}
\alias{ordbeta}
\alias{custom_family}
\title{Response families for generalized BART}
\usage{
negbin(link = "log", theta = NULL)

Gamma_shape(link = "log", shape = NULL)

ordinal(link = "logit")

multinomial(link = "logit", reference = NULL)

weibull_aft()

loglogistic_aft()

lognormal_aft()

location_scale(link = "identity")

zi_poisson(link = "log")

zi_negbin(link = "log", theta = NULL)

ordbeta(link = "logit", phi = NULL)

custom_family(
  logdens,
  num_predictors = 1L,
  start = 0,
  derivatives = NULL,
  name = "custom"
)
}
\arguments{
\item{link}{the link function. Each family compiles the links for which the
additive predictor is the natural unconstrained scale; any other link is
applied from R, for the families where that is well defined. See Details.}

\item{theta}{for \code{negbin()} and \code{zi_negbin()}, a fixed value for the
dispersion parameter. The default, \code{NULL}, draws it along with everything
else.}

\item{shape}{for \code{Gamma_shape()}, a fixed value for the gamma shape. The
default, \code{NULL}, draws it.}

\item{reference}{for \code{multinomial()}, the response category to hold as the
reference. The default, \code{NULL}, fits one forest per category instead and
leaves the model unidentified, which is what makes the prior symmetric in
the categories; see Details.}

\item{phi}{for \code{ordbeta()}, a fixed value for the beta precision. The
default, \code{NULL}, draws it.}

\item{logdens}{for \code{custom_family()}, the log density. A function of the
response and the additive predictors, \verb{function(y, eta)}, where \code{y} is a
numeric vector of length \code{n} and \code{eta} an \code{n} by \code{num_predictors} matrix,
returning a numeric vector of length \code{n}. It is the log density of \emph{one
unit of prior weight}, so that \code{weights} behave as they do elsewhere, and
terms free of \code{eta} may be dropped.}

\item{num_predictors}{for \code{custom_family()}, how many additive predictors the
density has, that is, how many forests to fit.}

\item{start}{for \code{custom_family()}, the value each additive predictor starts
at, in place of the intercept-only fit the compiled families use. One value
or one per predictor.}

\item{derivatives}{for \code{custom_family()}, an optional
\verb{function(y, eta, h)} returning a list with elements \code{score} and \code{info},
the first derivative of \code{logdens} with respect to the \code{h}th predictor and
minus its second derivative, each a vector of length \code{n}. The default,
\code{NULL}, takes central differences of \code{logdens}.}

\item{name}{for \code{custom_family()}, a label used when printing the fit.}
}
\value{
A list of class \code{genbart_family}, containing at least the elements \code{family}
and \code{link}. Objects of this class are recognized by \code{genbart()} alongside
ordinary \link[stats:family]{stats::family} objects.
}
\description{
\code{genbart()} accepts the \link[stats:family]{stats::family} objects used by \code{\link[stats:glm]{stats::glm()}}, so
\code{gaussian()}, \code{binomial("probit")}, \code{poisson()} and \code{Gamma("log")} all work
unchanged. The functions documented here supply the additional families that
have no \code{glm()} counterpart, in the same style, so that they can be passed to
the \code{family} argument the same way.
}
\details{
Every family reduces to a scalar additive predictor, or to several of them,
together with the first two derivatives of the log density with respect to
each. That is the whole interface the sampler needs, which is why the set of
available families is not restricted to the conditionally conjugate ones.

The supported families and links are:\tabular{llll}{
   Family \tab Links \tab Additive predictors \tab Drawn nuisance parameters \cr
   \code{gaussian()} \tab \code{identity} \tab 1 \tab residual standard deviation \cr
   \code{binomial()} \tab \code{logit}, \code{probit}, \code{cloglog} \tab 1 \tab none \cr
   \code{poisson()} \tab \code{log} \tab 1 \tab none \cr
   \code{negbin()} \tab \code{log} \tab 1 \tab dispersion \cr
   \code{Gamma()}, \code{Gamma_shape()} \tab \code{log} \tab 1 \tab shape \cr
   \code{ordinal()} \tab \code{logit}, \code{probit}, \code{cloglog} \tab 1 \tab cutpoints \cr
   \code{multinomial()} \tab \code{logit} \tab one per category, or per non-reference level \tab none \cr
   \code{weibull_aft()}, \code{loglogistic_aft()}, \code{lognormal_aft()} \tab none \tab 1 \tab scale \cr
   \code{location_scale()} \tab \code{identity} \tab 2 \tab none \cr
   \code{zi_poisson()} \tab \code{log} \tab 2 \tab none \cr
   \code{zi_negbin()} \tab \code{log} \tab 2 \tab dispersion \cr
   \code{ordbeta()} \tab \code{logit} \tab 1 \tab 2 cutpoints, precision \cr
}
}
\section{Links the engine does not compile}{
The links listed above are the ones the sampler evaluates in compiled code.
Any other link is accepted for \code{gaussian()}, \code{binomial()}, \code{poisson()},
\code{negbin()} and \code{Gamma()}, and applied from R: the additive predictor is
mapped to the scale the compiled family works on -- the mean, the log mean or
the log odds -- by composing the caller's inverse link with the family's own,
and the chain rule carries the derivatives back. So
\code{binomial("cauchit")} works, as does any link object of the kind
\code{\link[stats:make.link]{stats::make.link()}} returns, including one written by hand:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{my_link <- structure(
  list(linkfun = function(mu) qnorm(mu) / 2,
       linkinv = function(eta) pnorm(2 * eta),
       mu.eta = function(eta) 2 * dnorm(2 * eta),
       valideta = function(eta) TRUE, name = "half-probit"),
  class = "link-glm")

genbart(y ~ ., data = d, family = binomial(my_link))
}\if{html}{\out{</div>}}

Three things are worth knowing about this route. It costs a call into R for
every leaf the sampler visits, so a fit is slower than one with a compiled
link, though not by the factor a call per observation would cost. The leaf
prior scale is calibrated for the compiled link, so an unusual link may want
\code{sigma_mu} set by hand in \code{\link[=genbart_control]{genbart_control()}}. And the additive predictor is
unconstrained, so a link whose inverse has a restricted range --
\code{poisson("identity")}, \code{Gamma("inverse")} -- will produce non-finite
densities for some predictors; those proposals are rejected rather than
breaking the chain, but the fit will be poor. Prefer links whose inverse is
defined on the whole line.

The families with more than one additive predictor, or whose link enters
somewhere other than a single mean -- \code{ordinal()}, \code{multinomial()}, the
accelerated failure time families, \code{location_scale()}, the zero-inflated
families and \code{ordbeta()} -- take only their listed links. \code{custom_family()}
is the way to reach anything else.
}

\section{Supplying a likelihood}{
\code{custom_family()} takes the log density itself, as an R function, and fits
the model that goes with it. Nothing else about the sampler changes: the
leaf-level Laplace proposal needs the first two derivatives of the log
density with respect to each additive predictor and nothing more, and central
differences of the supplied function produce both.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# A Poisson model written out by hand. Terms free of eta may be dropped;
# they cancel from every acceptance ratio.
pois <- custom_family(function(y, eta) y * eta[, 1] - exp(eta[, 1]),
                      start = log(mean(d$y)))

# Two predictors: a mean and a log standard deviation.
ls <- custom_family(function(y, eta) dnorm(y, eta[, 1], exp(eta[, 2]),
                                           log = TRUE),
                    num_predictors = 2, start = c(0, 0))
}\if{html}{\out{</div>}}

The function is called once per leaf per Fisher-scoring step with the
observations reaching that leaf, so it must be vectorized over \code{y} and the
rows of \code{eta}; it must not be vectorized \emph{within} an observation, and it must
return exactly one value per row. Supplying \code{derivatives} cuts three calls to
one and removes the differencing error, and is worth doing when the
derivatives are easy to write down.

What \code{custom_family()} does not do: it cannot draw a nuisance parameter, so a
dispersion has to be fixed inside the closure; the response must be numeric,
so a factor has to be coded first; and since the package cannot know what the
mean of the density is, \code{predict(type = "response")} returns the additive
predictors rather than a fitted mean.
}

\section{Families in detail}{
\code{ordinal()} expects an ordered factor, though a numeric or integer response
is accepted and its sorted unique values are taken as the categories. It uses
the cumulative-link parameterization of \code{\link[MASS:polr]{MASS::polr()}}, in which
\eqn{P(Y \le k) = F(c_k - \eta)}, so that larger values of the additive
predictor shift mass towards higher categories.

Only the differences \eqn{c_k - \eta_i} are identified, so one location has
to be pinned. With three or more categories the draws are reported in the
chart where \strong{the additive predictor has mean zero over the fitted sample and
every cutpoint is free}, which is the convention \code{\link[MASS:polr]{MASS::polr()}} reports in
and makes the cutpoints directly readable as category boundaries. With exactly
two categories the single boundary is folded into the intercept instead, so a
two-category response is exactly binary regression with the matching link and
on the same scale.

The choice of chart is not a choice of model: the sampler works in whichever
is better conditioned and the shift is applied to the recorded predictor, the
recorded cutpoints and the recorded leaf values together, so every category
probability is untouched and the stored forest still replays to the reported
predictor. It does mean that \code{cut1} is a free parameter rather than a constant
zero, which is a change from earlier versions.

\strong{Comparing with \code{\link[MASS:polr]{MASS::polr()}}.} This is the chart \code{polr()} reports in when
its predictors are centered. \code{polr()} identifies the location by leaving the
intercept out of the design matrix rather than by centering, so its \code{zeta} is
shifted by the mean of its own linear predictor, which is not zero unless the
predictors happen to be. Either of these puts the two side by side:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fit <- genbart(y ~ x1 + x2, data = d, family = ordinal("probit"))
colMeans(fit$aux)                       # cutpoints, predictor centered

p <- MASS::polr(y ~ x1 + x2, data = d, method = "probit")
p$zeta - mean(p$lp)                     # the same chart

d2 <- transform(d, x1 = x1 - mean(x1), x2 = x2 - mean(x2))
MASS::polr(y ~ x1 + x2, data = d2, method = "probit")$zeta   # likewise
}\if{html}{\out{</div>}}

On a linear truth with n = 3000 the three agree to about 0.02, which is Monte
Carlo error; there is a test to that effect. The \emph{gaps} between cutpoints are
identified outright and match whatever chart either is in.

All three links are fitted through a latent variable, which
\code{\link[=genbart_control]{genbart_control()}}'s \code{augment} uses by default: conditional on it the target
over a leaf is quadratic and the sampler takes the closed form instead of
Fisher scoring plus a Metropolis ratio. For \code{ordinal("probit")} the latent
variable is normal (Albert and Chib 1993) and the measured gain on a thousand
observations and fifty trees is 14 times with soft rules and 30 times with
hard ones. For \code{ordinal("logit")} it is logistic, which is a normal whose
precision is Polya-Gamma, and the gain is 7 times with soft rules and 15 with
hard ones -- smaller because the precision is an extra draw per observation.
For \code{ordinal("cloglog")} it is an exponential waiting time, because the
complementary log-log model \emph{is} the discrete proportional hazards model; the
gain is 5 times with hard rules and about 1.6 times in effective samples per
second with soft ones, since that target is the exponential form rather than
the quadratic one and the exponential form needs hard rules. Accuracy is
unchanged in every case. So the link can be chosen on modelling grounds; see
\code{\link[=genbart_control]{genbart_control()}} for the details and for how to turn the rewriting off.

\code{Gamma_shape()} is the gamma family again, and differs from \code{stats::Gamma()}
only in taking a \code{shape} argument: the shape, which acts as the inverse
dispersion, is drawn by default and can instead be fixed. It does \emph{not}
regress the shape on the predictors. \code{negbin()} and \code{ordbeta()} take
\code{theta} and \code{phi} the same way.

\code{multinomial()} expects an unordered factor. By default it fits one forest
per category and leaves the model unidentified, since adding any function of
the predictors to every category's forest leaves the probabilities alone.
This is the parameterization of Murray (2021): the leaf prior is proper, so
the posterior is proper too, and every identified quantity -- a probability,
an odds ratio -- is recovered from the draws. Its point is that the prior is
then symmetric in the categories, whereas reference coding makes the fit
depend on which category was singled out. The leaf prior scale is divided by
\eqn{\sqrt 2} to compensate for each log-odds contrast now being a
difference of two forests, which leaves the prior on the identified log odds
where reference coding puts it.

Passing \code{reference} instead pins that category's predictor at zero and fits
\code{num_cat - 1} forests, so the fitted functions are log odds against it and
are interpretable the way the coefficients from \code{\link[nnet:multinom]{nnet::multinom()}} are. That
is one fewer forest, and the right choice when a particular contrast is the
quantity of interest.

The accelerated failure time families expect a right-censored response,
supplied either as a \code{\link[survival:Surv]{survival::Surv()}} object or as a two-column matrix of
times and event indicators. They model \eqn{\log T = \eta + \sigma\epsilon}
with \eqn{\epsilon} standard Gumbel, logistic or normal respectively, giving
Weibull, log-logistic and log-normal survival times.

\code{location_scale()} regresses the mean and the log standard deviation of a
normal response on separate forests, so the variance is an unrestricted
function of the predictors.

\code{zi_poisson()} and \code{zi_negbin()} are zero-inflated counts, mixing a point
mass at zero with a Poisson or negative binomial count component. Both parts
get their own forest: the first predictor is the log mean of the count
component and the
second the log odds that an observation is a structural zero, so the
excess-zero mechanism is free to depend on the predictors rather than being a
single constant. The two are reported as the \code{count} and \code{zero} predictors.

\code{ordbeta()} is the ordered beta regression of Kubinec (2023), for a response
on the closed unit interval with point masses at zero and one, such as a
proportion or a slider scale. One predictor drives both the probability of
landing on an endpoint, through a pair of cutpoints as in an ordinal model,
and the mean of the beta density in between:
\deqn{P(Y = 0) = 1 - \mathrm{logit}^{-1}(\eta - c_1),}
\deqn{P(Y = 1) = \mathrm{logit}^{-1}(\eta - c_2),}
with the remaining mass following a
\eqn{\mathrm{Beta}(\mu\phi, (1 - \mu)\phi)} density for
\eqn{\mu = \mathrm{logit}^{-1}(\eta)}. Because the predictor also enters
the beta mean it is identified, so unlike \code{ordinal()} both cutpoints are
drawn.

Proportional hazards regression is deliberately absent. Its partial
likelihood couples observations through risk sets, so it does not decompose
into a sum over the observations reaching a leaf, which is what the leafwise
Laplace approximation requires. The accelerated failure time families cover
the same ground within this framework.
}

\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
genbart(y ~ ., data = d, family = negbin())
genbart(y ~ ., data = d, family = ordinal("probit"))
genbart(survival::Surv(time, status) ~ ., data = d, family = weibull_aft())
genbart(count ~ ., data = d, family = zi_negbin())
genbart(proportion ~ ., data = d, family = ordbeta())

# A link the engine does not compile, applied from R.
genbart(y ~ ., data = d, family = binomial("cauchit"))

# A likelihood supplied from R.
genbart(y ~ ., data = d,
        family = custom_family(function(y, eta) y * eta[, 1] - exp(eta[, 1])))
\dontshow{\}) # examplesIf}
}
\references{
Kubinec, R. (2023). Ordered beta regression: a parsimonious, well-fitting
model for continuous data with lower and upper bounds. \emph{Political Analysis},
31(4), 519--536. \doi{10.1017/pan.2022.20}

Murray, J. S. (2021). Log-linear Bayesian additive regression trees for
multinomial logistic and count regression models. \emph{Journal of the American
Statistical Association}, 116(534), 756--769.
\doi{10.1080/01621459.2020.1813587}
}
\seealso{
\code{\link[=genbart]{genbart()}}
}
