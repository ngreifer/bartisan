% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{.bartisan_tree_uses}
\alias{.bartisan_tree_uses}
\title{Which stored trees split on a given set of predictor columns}
\usage{
.bartisan_tree_uses(
  forest_flat,
  tree_start,
  num_forest,
  num_trees,
  num_draws,
  num_cols,
  cat_cols
)
}
\arguments{
\item{forest_flat, tree_start}{the encoded forests returned by
\code{.bartisan_fit()}.}

\item{num_forest, num_trees, num_draws}{\code{integer}; the dimensions of the
stored chain.}

\item{num_cols}{\code{integer}; the zero-based columns of the design matrix to
look for, for rules without a level mask.}

\item{cat_cols}{\code{integer}; the zero-based columns of the level-code matrix to
look for, for rules with one.}
}
\value{
A \code{logical} vector with one entry per stored tree, in the order the
flat encoding holds them, which is iteration-major and then forest and then
tree.
}
\description{
A sum of trees is separable, so a tree that never splits on a column
contributes the same amount however that column is set. Partial dependence
evaluates one column over a grid with the rest of the data held fixed, so the
trees this marks out are the only ones that have to be re-evaluated at each
grid point; the rest are evaluated once.
}
\keyword{internal}
