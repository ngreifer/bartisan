% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginaleffects.R
\name{bartisan-marginaleffects}
\alias{bartisan-marginaleffects}
\alias{formula.bartisan_fit}
\alias{terms.bartisan_fit}
\alias{model.frame.bartisan_fit}
\alias{nobs.bartisan_fit}
\alias{family.bartisan_fit}
\alias{get_predict.bartisan_fit}
\alias{get_group_names.bartisan_fit}
\alias{get_coef.bartisan_fit}
\alias{set_coef.bartisan_fit}
\alias{get_vcov.bartisan_fit}
\alias{get_data.bartisan_fit}
\title{Counterfactual estimands with marginaleffects}
\usage{
\method{formula}{bartisan_fit}(x, ...)

\method{terms}{bartisan_fit}(x, ...)

\method{model.frame}{bartisan_fit}(formula, ...)

\method{nobs}{bartisan_fit}(object, ...)

\method{family}{bartisan_fit}(object, ...)

\method{get_predict}{bartisan_fit}(model, newdata = NULL, type = NULL, ...)

\method{get_group_names}{bartisan_fit}(model, ...)

\method{get_coef}{bartisan_fit}(model, ...)

\method{set_coef}{bartisan_fit}(model, coefs, ...)

\method{get_vcov}{bartisan_fit}(model, ...)

\method{get_data}{bartisan_fit}(x, ...)
}
\arguments{
\item{...}{further arguments. \code{values}, \code{iterations}, \code{offset}, \code{weights},
\code{log} and \code{times} are passed on to \code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}}; anything else
is ignored, since \pkg{marginaleffects} puts arguments of its own here too.
Note that \pkg{marginaleffects} warns that it does not recognize \code{values},
which is expected (it is this package's argument, not one of its own), and
the value is used regardless.}

\item{model, x, object, formula}{a \verb{<bartisan_fit>} object; the output of a call
to \code{\link[=bartisan]{bartisan()}}. The last is named \code{formula} only because
\code{\link[stats:model.frame]{stats::model.frame()}} names its first argument that way.}

\item{newdata}{optional; a data frame at which to evaluate the fit. Default
is \code{NULL} to use the data the model was fitted to, which is retained in the
fit for this purpose.}

\item{type}{string; the scale to work on. Allowable options include
\code{"response"} (the default), \code{"link"}, \code{"prob"}, \code{"mean"}, \code{"stdlv"}, and
\code{"survival"}. \code{"response"} is the fitted mean, \code{"link"} the additive
predictor, \code{"prob"} the per-category probabilities of a categorical family,
\code{"mean"} the mean of a categorical response with its labels read as
numbers, \code{"stdlv"} the standardized latent variable of an ordinal fit, and
\code{"survival"} the survival function at the times given in \code{times}; all but
the first two are described under \code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}}. An \code{ordinal()}
or \code{multinomial()} response has no single mean, so \code{"response"} gives
\code{"prob"} for those families, while a binomial fit reports the probability
of its second level, as \code{\link[stats:glm]{stats::glm()}} does, and \code{"prob"} is what asks for
both of its columns. \code{"probs"}, \code{"lp"}, \code{"lv"} and \code{"surv"} are accepted as
aliases for \code{"prob"}, \code{"link"}, \code{"link"} and \code{"survival"}, since those are
the names the same quantities go by for other ordinal fits in
\pkg{marginaleffects}.}

\item{coefs}{ignored; a forest has no coefficient vector.}
}
\value{
\code{get_predict()} returns a data frame with columns \code{rowid}, \code{group} and
\code{estimate}, carrying the draws in a \code{"posterior_draws"} attribute of
observations by draws, which is the interface \pkg{marginaleffects}
documents. The other methods exist to satisfy the generic and return what
their names suggest.
}
\description{
Methods that let a \verb{<bartisan_fit>} object be used by the
\pkg{marginaleffects} package, which computes predictions, comparisons and
slopes (and hypothesis tests on any of them) from the posterior draws. There
is nothing to set up: load \pkg{marginaleffects} and call its functions on
the fit.
}
\details{
\subsection{Uncertainty}{

A forest has no coefficient vector and no variance-covariance matrix, so the
delta method \pkg{marginaleffects} uses for a frequentist model has nothing to
work with. It has the posterior draws instead: every estimand is computed by
pushing all of the draws through the same transformation and summarizing at
the end, so an interval is a posterior quantile rather than a normal
approximation, and a nonlinear estimand needs no approximation at all. This is
the same path \pkg{marginaleffects} takes for \pkg{brms} and \pkg{rstanarm}
fits.

One consequence is worth knowing: \pkg{marginaleffects} centers a posterior at
its median, where \code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}} reports its mean. The two summarize
the same draws, so a difference between them is the skewness of the posterior
rather than a disagreement.
}

\subsection{A Contrast of Exactly Zero Is Usually Real}{

\code{avg_comparisons()} reporting an estimate of exactly \code{0} is the most common
surprise here, and two facts meet to produce it.

The posterior of a contrast has an atom at exactly zero. In any draw where no
tree splits on the variable being contrasted, the fit does not depend on that
variable at all, so the two counterfactual predictions are identical to the
last bit and their difference is exactly zero. The Dirichlet sparsity prior,
\code{sparsity} in \code{\link[=bartisan_control]{bartisan_control()}}, is what makes those draws common, since
dropping a weak predictor from every tree is what a variable selection prior
is for. And \pkg{marginaleffects} centers a posterior at its median, so once
the atom holds more than half the mass the reported estimate is exactly zero
however large the rest of the posterior is: the median can land on the atom
while the posterior mean and the upper limit of the interval are both far from
zero.

Four things are worth doing about it, in the order given. Look at
\code{prop_used} in \code{summary()}, the posterior probability that the predictor
appears anywhere in the forest, which is what the zero reports. Ask for the
mean instead, with \code{options(marginaleffects_posterior_center = mean)}, which
is the summary \code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}} reports and the one that behaves
sensibly against an atom. Reconsider the sparsity prior, since
\code{sparsity = FALSE} removes the atom almost entirely where a larger \code{num_trees}
leaves it in place; that is a modeling choice rather than a fix, and it is the
right one when a contrast on a particular predictor is the estimand. And run
several chains and compare them, because the variable selection state mixes
slowly and a single chain can look much more settled than the posterior is.
\code{vignette("effects")} works the choice through.
}

\subsection{Slopes and the Predictor Transform}{

A slope is a numerical derivative, and taking one requires the fitted function
to be differentiable in the predictor as the caller supplies it. Whether it is
depends on \code{x_transform} in \code{\link[=bartisan_control]{bartisan_control()}}, because the fit is a smooth
function of the transformed predictor rather than of the original one.

\code{x_transform = "range"} is what slopes want, and refitting with it is
worthwhile when a derivative is the quantity being reported rather than a
prediction. It is the only affine map of the three, so it is the only one
through which a slope of the fit is a slope of the original predictor. The
default, \code{"smoothcdf"}, carries an estimated density along with it and lands
close; \code{"quantile"} maps each predictor through a step function and so has no
derivative to take, and the number returned there is a property of the step
size rather than of the fit. \code{vignette("effects")} shows what each does as the
step shrinks.

None of this affects \pkgfun{marginaleffects}{predictions} or
\pkgfun{marginaleffects}{comparisons}, which evaluate the fit at two points a
substantive distance apart rather than dividing by a vanishing one.
}

\subsection{The Usual Survival Estimand}{

For a survival family, the estimand is usually a contrast in survival at a
horizon rather than in the predictor. \code{type = "survival"} with \code{times} gives
it:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# The difference in one-year survival between treated and untreated
avg_comparisons(fit, variables = "trt", type = "survival", times = 1)
}\if{html}{\out{</div>}}

One time per call. \pkg{marginaleffects} checks the dots against a whitelist
of its own, hardcoded per model class, so it warns that it does not recognize
\code{times} while passing it through, which is what the warning says. There is no
hook for registering an argument with it, so the warning is expected and the
result is correct.
}

\subsection{Prediction Types}{

\code{type = "link"} needs a family with a single additive predictor, since there
is no one link to be talking about in \code{gaussian_ls()}, the zero-inflated
families or \code{multinomial()}. Those families work on the response scale, which
is one number per observation whatever the family, and \code{multinomial()} and
\code{ordinal()} work on the probability scale, which gives one group per category.
\code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}} reaches a particular predictor of a multi-predictor
family, and computes \code{type = "class"} and \code{type = "density"}, which are not
one number per observation that an average could be taken of.

Extrapolation is worth keeping in mind for \code{comparisons()}: a forest is
constant outside the range of the predictor it was fitted on, so a contrast
that steps a predictor beyond that range reports the boundary value rather
than an extrapolated one.
}
}
\examples{
\dontshow{if (rlang::is_installed("marginaleffects")) withAutoprint(\{ # examplesIf}
data("rhc")
set.seed(123)

# Whether a patient died, with every other variable a candidate predictor.
# The sparsity prior is turned off because a contrast on one predictor is
# the estimand rather than variable selection
fit <- bartisan(death ~ . - days, data = rhc, sparsity = FALSE,
                num_trees = 10, num_burn = 50, num_draws = 50)

# The effect of catheterization on the probability of death, as an average
# contrast of counterfactual predictions
marginaleffects::avg_comparisons(fit, variables = "rhc")

# The same contrast within each sex, and a test that the two are equal
marginaleffects::avg_comparisons(fit, variables = "rhc", by = "sex",
                                 hypothesis = ~pairwise)

# Centering each posterior at its mean rather than its median, which is the
# summary `predict()` reports
op <- options(marginaleffects_posterior_center = mean)
marginaleffects::avg_comparisons(fit, variables = "rhc")
options(op)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}} for the prediction scales these estimands are
computed on; \code{\link[=bartisan_control]{bartisan_control()}} for \code{sparsity} and \code{x_transform};
\code{\link{bartisan-interop}} for the methods that let other packages assess the fit
}
