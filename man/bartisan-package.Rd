% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bartisan-package.R
\docType{package}
\name{bartisan-package}
\alias{bartisan-package}
\title{bartisan: Generalized Bayesian Additive Regression Trees}
\description{
Fits Bayesian additive regression trees (BART) models for likelihoods outside the conditionally conjugate Gaussian case, using the Laplace-approximation reversible-jump sampler of Linero (2025). Supported response families include Gaussian, binomial, Poisson, negative binomial, gamma, ordinal (cumulative link), multinomial, accelerated failure time models for right-censored survival data, and location-scale Gaussian regression. Decision rules may be hard, as in standard BART, or soft, as in the SoftBart model of Linero and Yang (2018), which yields smoother fits. The interface follows that of 'glm()', so that a model is specified with a formula, a data frame, and a family.
}
\details{
Fits Bayesian additive regression trees the way \code{\link[stats:glm]{stats::glm()}} fits a
generalized linear model: a formula, a data frame, and a family. The forest
replaces the linear predictor, so nothing has to be said about which terms
enter, which are curved, or which interact. Everything else about the workflow
stays where it was.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fit <- bartisan(y ~ ., data = d)
}\if{html}{\out{</div>}}
}
\section{What to reach for}{
Most of what you will want to do with a fitted model lives in a package that
already does that job well, and \emph{bartisan} registers the methods those packages
need rather than reimplementing them. This table is the map.\tabular{ll}{
   You want to \tab Use \cr
   fit a model \tab \code{\link[=bartisan]{bartisan()}} \cr
   choose a likelihood \tab \link{bartisan-families}, \code{vignette("families")} \cr
   change the sampler's settings \tab \code{\link[=bartisan_control]{bartisan_control()}} \cr
   predict for new data \tab \code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}} \cr
   a prediction \strong{with an interval} \tab \code{marginaleffects::predictions()} \cr
   an interval for a \strong{new observation}, noise included \tab \link[=bartisan-interop]{posterior_predict()} \cr
   how much a predictor moves the outcome \tab \code{marginaleffects::avg_comparisons()} \cr
   a partial dependence plot \tab \code{marginaleffects::plot_predictions()} \cr
   which predictors the forest uses \tab \code{\link[=variable_importance]{variable_importance()}} \cr
   to check it converged \tab \code{fit$rhat}, then \link[=bartisan-interop]{as_draws()} with \pkg{bayesplot} \cr
   to check it fits \tab \link[=bartisan-interop]{pp_check()}, \link[=bartisan-interop]{residuals()} \cr
   to compare two models \tab \link[=bartisan-interop]{loo()} \cr
   survival data \tab \code{\link[=ph]{ph()}}, \code{\link[=dpm_aft]{dpm_aft()}}, \code{vignette("survival")} \cr
   a likelihood of your own \tab \code{\link[=custom_family]{custom_family()}} \cr
}
}

\section{If you are new to this}{
Three things are worth knowing before the first fit, and none of them requires
knowing anything about Bayesian statistics.

\strong{There are no coefficients.} A forest has no slope to read off, so the
question "what is the effect of \code{x}" is answered by asking the fitted model
what it predicts under one value of \code{x} and under another, and taking the
difference. \code{marginaleffects::avg_comparisons(fit, variables = "x")} does
exactly that, and reports an interval with it. This is a better habit than
reading coefficients even when coefficients exist, and here it is the only
habit available.

\strong{The intervals mean what you would hope.} A 95\% interval from any of the
above is the range the model considers most plausible, given the data and the
model. It already includes the uncertainty from not knowing the shape of the
relationship, which is the part a linear model leaves out by assuming it away.

\strong{The defaults are meant to be used.} The settings in \code{\link[=bartisan_control]{bartisan_control()}} are
there for people who need them; the priors and the number of trees are chosen
to work across a wide range of problems, and tuning them is rarely where the
gains are. Choosing the right \link[=bartisan-families]{family} matters much more.
}

\section{What the fit does not do for you}{
It is flexible about the shape of the relationship, not about anything else.
It will not tell you that a predictor is a cause, that the sample represents
the population, or that the outcome was measured well. A forest fitted to
confounded data returns a confounded answer with a tight interval around it.
}

\seealso{
\code{\link[=bartisan]{bartisan()}} to fit, \link{bartisan-families} to choose a likelihood,
\link{bartisan-marginaleffects} and \link{bartisan-interop} for the packages that read a
fit, and \code{vignette("bartisan")} for how the sampler works.
}
\author{
\strong{Maintainer}: Noah Greifer \email{noah.greifer@gmail.com} (\href{https://orcid.org/0000-0003-3067-7154}{ORCID})

Authors:
\itemize{
  \item Noah Greifer \email{noah.greifer@gmail.com} (\href{https://orcid.org/0000-0003-3067-7154}{ORCID})
}

Other contributors:
\itemize{
  \item Antonio R. Linero (Author of the FlexBart reference implementation from which the MCMC engine is adapted) [contributor, copyright holder]
}

}
\keyword{internal}
