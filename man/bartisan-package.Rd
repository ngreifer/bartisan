% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bartisan-package.R
\docType{package}
\name{bartisan-package}
\alias{bartisan-package}
\title{bartisan: Generalized Bayesian Additive Regression Trees}
\description{
Fits Bayesian additive regression trees (BART) models for likelihoods outside the conditionally conjugate Gaussian case, using the Laplace-approximation reversible-jump sampler of Linero (2025) \doi{10.1080/01621459.2024.2337156}. Supported response families include Gaussian, binomial, Poisson, negative binomial, gamma, beta, ordered beta, ordinal (cumulative link), multinomial, zero-inflated counts, a Tweedie compound Poisson for a non-negative response with a point mass at zero, accelerated failure time and proportional hazards models for right-censored survival data, location-scale regression, and a Dirichlet process mixture for the error distribution. Decision rules may be hard, as in standard BART, or soft, as in the SoftBart model of Linero and Yang (2018) \doi{10.1111/rssb.12293}, which yields smoother fits. The interface follows that of 'glm()', so that a model is specified with a formula, a data frame, and a family.
}
\details{
Fits Bayesian additive regression trees (BART) the way \code{\link[stats:glm]{stats::glm()}} fits a
generalized linear model: a formula, a data frame, and a family. The forest
replaces the linear predictor, so nothing has to be said about which terms
enter the model, which are curved, or which interact.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fit <- bartisan(y ~ x1 + x2 + x3, data = d)
}\if{html}{\out{</div>}}
\subsection{Common Tasks}{

Most of what a fitted model is used for is handled by a package that already
does that job well, and \pkg{bartisan} registers the methods those packages
need. This table is the map.\tabular{ll}{
   Task \tab Use \cr
   fit a model \tab \code{\link[=bartisan]{bartisan()}} \cr
   choose a likelihood \tab \code{\link{bartisan-families}}, \code{vignette("families")} \cr
   change the sampler's settings \tab \code{\link[=bartisan_control]{bartisan_control()}} \cr
   predict for new data \tab \code{\link[=predict.bartisan_fit]{predict.bartisan_fit()}} \cr
   a prediction with an interval \tab \pkgfun{marginaleffects}{predictions} \cr
   an interval for a new observation, noise included \tab \code{\link[=bartisan-interop]{posterior_predict()}} \cr
   how much a predictor moves the outcome \tab \pkgfun{marginaleffects}{avg_comparisons}, \code{vignette("effects")} \cr
   a partial dependence plot \tab \code{\link[=partial_dependence]{partial_dependence()}} \cr
   which predictors the forest uses \tab \code{\link[=variable_importance]{variable_importance()}} \cr
   to tell the prior which predictors matter \tab \code{split_prior} in \code{\link[=bartisan_control]{bartisan_control()}} \cr
   to see what prior a fit was given \tab \code{\link[=bartisan-interop]{prior_summary()}} \cr
   to see what that prior implies about the outcome \tab \code{prior_only} in \code{\link[=bartisan]{bartisan()}} \cr
   to give one forest its own predictors or settings \tab a list of formulas, and per-forest arguments; see \code{\link{bartisan-families}} \cr
   a coefficient that varies with the other predictors \tab \code{\link[=vc]{vc()}} in the formula, \code{vignette("varying")} \cr
   a treatment effect with its own prior \tab \code{\link[=bcf]{bcf()}}, \code{vignette("causal")} \cr
   the ATE, ATT or the effect for each unit \tab \code{\link[=estimate_effect]{estimate_effect()}} \cr
   to check it converged and mixed \tab \code{\link[=diagnose]{diagnose()}}, then \code{\link[=bartisan-interop]{as_draws()}} with \pkg{bayesplot}, \code{vignette("diagnostics")} \cr
   to check it fits \tab \code{\link[=bartisan-interop]{pp_check()}}, \code{\link[=bartisan-interop]{residuals()}} \cr
   to compare two models \tab \code{\link[=bartisan-interop]{loo()}}, \code{vignette("comparison")} \cr
   survival data \tab \code{\link[=ph]{ph()}}, \code{\link[=dpm_aft]{dpm_aft()}}, \code{vignette("survival")} \cr
   a likelihood of one's own \tab \code{\link[=custom_family]{custom_family()}} \cr
}

}

\subsection{Before the First Fit}{

There are a few things worth knowing first, and none of them requires
knowing anything about Bayesian statistics.

A forest has no slope to read off, so the effect of a predictor is found by
asking the fitted model what it predicts under one value of that predictor
and under another, and taking the difference.
\code{marginaleffects::avg_comparisons(fit, variables = "x")} does exactly that,
and reports an interval with it. This is a better habit than reading
coefficients even where coefficients exist (i.e., in a linear model), and
here it is the only habit available.

A 95\% interval from any of the above is the range the model considers most
plausible given the data and the model. It already includes the uncertainty
from not knowing the shape of the relationship, which is the part a linear
model leaves out by assuming it away.

The defaults are meant to be used. The settings in \code{\link[=bartisan_control]{bartisan_control()}}
are there for the analyses that need them; the priors and the number of trees
are chosen to work across a wide range of problems, and tuning them is rarely
where the gains are. Choosing the right \link[=bartisan-families]{family} matters
much more.
}

\subsection{Flexibility and Its Limits}{

The model is flexible about the shape of the relationship and about nothing
else. Whether a predictor is a cause, whether the sample represents the
population, and whether the outcome was measured well are assumptions the
analysis brings to the model rather than things it can supply: a forest
fitted to confounded data returns a confounded answer with a tight interval
around it.
}
}
\seealso{
\code{\link[=bartisan]{bartisan()}} to fit a model; \code{\link{bartisan-families}} to choose a likelihood;
\code{\link{bartisan-marginaleffects}} and \code{\link{bartisan-interop}} for the packages that read a
fit; \code{vignette("bartisan")} for a worked analysis and
\code{vignette("implementation")} for how the sampler works.
}
\author{
\strong{Maintainer}: Noah Greifer \email{noah.greifer@gmail.com} (\href{https://orcid.org/0000-0003-3067-7154}{ORCID})

Authors:
\itemize{
  \item Noah Greifer \email{noah.greifer@gmail.com} (\href{https://orcid.org/0000-0003-3067-7154}{ORCID})
}

Other contributors:
\itemize{
  \item Antonio R. Linero (Author of the FlexBart reference implementation from which the MCMC engine is adapted) [contributor, copyright holder]
}

}
