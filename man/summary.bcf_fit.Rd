% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcf-methods.R
\name{summary.bcf_fit}
\alias{summary.bcf_fit}
\alias{print.summary.bcf_fit}
\alias{print.bcf_fit}
\alias{plot.bcf_fit}
\title{Methods for Bayesian causal forest fits}
\usage{
\method{summary}{bcf_fit}(
  object,
  level = 0.95,
  estimand = "ATE",
  comparison = "difference",
  interval = "eti",
  focal = NULL,
  ...
)

\method{print}{summary.bcf_fit}(x, digits = 3L, ...)

\method{print}{bcf_fit}(x, digits = 3L, ...)

\method{plot}{bcf_fit}(x, level = 0.95, comparison = "difference", ...)
}
\arguments{
\item{object, x}{a \verb{<bcf_fit>} object; the output of a call to \code{\link[=bcf]{bcf()}}.}

\item{level}{\code{numeric}; the level of the credible interval. Default is \code{.95}.}

\item{estimand, comparison, interval, focal}{passed to \code{\link[=estimate_effect]{estimate_effect()}},
which is what computes the numbers.}

\item{...}{for \code{summary()}, further arguments passed to
\code{\link[=estimate_effect]{estimate_effect()}}; otherwise ignored.}

\item{digits}{\code{integer}; the number of significant digits to print.}
}
\value{
\code{summary()} returns a \verb{<summary.bcf_fit>} object carrying the effect, the
marginal mean of each potential outcome, and the quartiles of the conditional
effects. \code{plot()} returns a \pkg{ggplot2} object. Both \code{print()} methods
return their input invisibly.
}
\description{
A fit from \code{\link[=bcf]{bcf()}} is a \verb{<bartisan_fit>} with a treatment named, so it takes
every method that fit takes and adds the ones that need a treatment.
\code{summary()} reports the effect rather than the forests, and \code{plot()} draws the
conditional effects.
}
\details{
\code{summary()} is \code{\link[=estimate_effect]{estimate_effect()}} with defaults and a compact printout, so
anything it does not report is available by calling that function directly:
another estimand, a ratio rather than a difference, subgroup effects, or the
posterior draws themselves.

The potential outcomes are printed beside the effect because a difference
means different things against different baselines, which is the point
\code{vignette("causal")} makes about earnings.
}
\examples{
\dontshow{if (rlang::is_installed("ggplot2")) withAutoprint(\{ # examplesIf}
data("rhc")
set.seed(123)

fit <- bcf(death ~ age + sex + meanbp + aps, treatment = ~ rhc,
           data = rhc, num_trees = 10, num_burn = 50, num_draws = 50,
           verbose = FALSE)

fit

summary(fit)

# The effect on the treated instead, still through `summary()`
summary(fit, estimand = "ATT")

# The conditional effects, ordered, with the average behind them
plot(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=estimate_effect]{estimate_effect()}} for the estimands and for what \code{summary()} does
not cover; \code{\link[=bcf]{bcf()}} for the model
}
