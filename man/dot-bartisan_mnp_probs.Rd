% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{.bartisan_mnp_probs}
\alias{.bartisan_mnp_probs}
\title{Category probabilities of a multinomial probit fit, by simulation}
\usage{
.bartisan_mnp_probs(eta_draws, sigma, replicates)
}
\arguments{
\item{eta_draws}{a list of one draws-by-observations matrix per latent
variable.}

\item{sigma}{a matrix of draws by the lower triangle of the covariance
matrix, column-major within a row, as \code{aux} stores it.}

\item{replicates}{simulation replicates per draw and observation.}
}
\value{
An array of draws by observations by categories.
}
\description{
The probability that the argmax of a correlated Gaussian vector falls in each
category is an orthant probability with no closed form, so it is simulated.
Fresh draws are taken on every call, which makes the estimate unbiased; the
Monte Carlo error is then averaged down by the posterior draws, so a modest
number of replicates per draw is enough for a posterior mean.
}
\keyword{internal}
