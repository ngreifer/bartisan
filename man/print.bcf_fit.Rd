% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcf-methods.R
\name{print.bcf_fit}
\alias{print.bcf_fit}
\alias{plot.bcf_fit}
\title{Methods for Bayesian causal forest fits}
\usage{
\method{print}{bcf_fit}(x, digits = 3L, ...)

\method{plot}{bcf_fit}(x, level = 0.95, comparison = "difference", ...)
}
\arguments{
\item{x}{a \verb{<bcf_fit>} object; the output of a call to \code{\link[=bcf]{bcf()}}.}

\item{digits}{\code{integer}; the number of significant digits to print.}

\item{...}{for \code{plot()}, further arguments passed to \code{\link[=estimate_effect]{estimate_effect()}};
otherwise ignored.}

\item{level}{\code{numeric}; the level of the credible interval. Default is \code{.95}.}

\item{comparison}{passed to \code{\link[=estimate_effect]{estimate_effect()}}, which is what computes the
numbers.}
}
\value{
\code{plot()} returns a \pkg{ggplot2} object. \code{print()} returns its input
invisibly.
}
\description{
A fit from \code{\link[=bcf]{bcf()}} is a \verb{<bartisan_fit>} with a treatment named, so it takes
every method that fit takes. \code{print()} adds the treatment and says where the
effect comes from, and \code{plot()} draws the conditional effects.
}
\details{
The effect itself is \code{\link[=estimate_effect]{estimate_effect()}}'s to report, and \code{summary()} on a
\verb{<bcf_fit>} is the same summary of the forests it is on any other fit, with a
line at the end saying so. That way the same call prints the same thing
whether the model came from \code{\link[=bcf]{bcf()}} or from \code{\link[=bartisan]{bartisan()}} with a \code{\link[=vc]{vc()}} term.
}
\examples{
data("rhc")
set.seed(123)

fit <- bcf(death ~ age + sex + meanbp + aps, treat = ~ rhc,
           data = rhc, num_trees = 10, num_burn = 50, num_draws = 50,
           verbose = FALSE)

fit

# The effect, with the potential outcomes it is a difference of
estimate_effect(fit)

# The conditional effects, ordered, with the average beside them
plot(fit)

}
\seealso{
\code{\link[=estimate_effect]{estimate_effect()}} for the effect and its estimands; \code{\link[=bcf]{bcf()}} for the
model
}
