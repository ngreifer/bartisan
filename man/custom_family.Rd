% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom_family.R
\name{custom_family}
\alias{custom_family}
\title{Fit a model to a likelihood written in R}
\usage{
custom_family(
  logdens,
  num_predictors = 1L,
  start = 0,
  derivatives = NULL,
  aux_names = NULL,
  aux_start = NULL,
  name = "custom"
)
}
\arguments{
\item{logdens}{the log density, given as a function of the response and the
additive predictors, \verb{function(y, eta)}, where \code{y} is a numeric vector of
length \code{n} and \code{eta} an \code{n} by \code{num_predictors} matrix, returning a numeric
vector of length \code{n}. With nuisance parameters it takes a third argument,
\verb{function(y, eta, aux)}, where \code{aux} is a numeric vector of their current
values. It is the log density of one unit of prior weight, so that
\code{weights} behave as they do elsewhere, and terms free of \code{eta} may be
dropped.}

\item{num_predictors}{\code{numeric}; how many additive predictors the density has
(i.e., how many forests to fit). Default is 1.}

\item{start}{\code{numeric}; the value each additive predictor starts at, in place
of the intercept-only fit the compiled families use. One value or one per
predictor. Default is 0.}

\item{derivatives}{optional; a \verb{function(y, eta, h)} returning a list with
elements \code{score} and \code{info}, the first derivative of \code{logdens} with respect
to the \code{h}th predictor and minus its second derivative, each a vector of
length \code{n}. Default is \code{NULL} to take central differences of \code{logdens}. It
covers the additive predictors only: a nuisance parameter is always
differenced, which costs three calls per sweep rather than three per leaf.}

\item{aux_names}{optional \code{character}; the names of the nuisance parameters
to draw, if any. Naming them is what declares them, because the names label
the columns of \code{fit$aux} and are what \code{summary()} and \code{\link[=diagnose]{diagnose()}} report
them under. They must be distinct and non-empty. Default is \code{NULL} for
none, unless \code{aux_start} is given, in which case the parameters are named
by \code{names(aux_start)} when it carries names and positionally (\code{"aux1"},
\code{"aux2"}, and so on) when it does not.}

\item{aux_start}{optional \code{numeric}; the value each nuisance parameter starts
at, given as one value or one per parameter. Default is \code{NULL}, which is 0
for each. The sampler will walk to the posterior from a poor start, so this
need only be the right order of magnitude. Supplying it is a second way to
declare the parameters, so \code{aux_start = c(shape = 1)} both names one and
starts it at 1.}

\item{name}{string; a label used when printing the fit. Default is
\code{"custom"}.}
}
\value{
A \verb{<bartisan_family>} object, which is a list containing at least the
elements \code{family} and \code{link} and which inherits from \code{family}, so that
\code{\link[=bartisan]{bartisan()}} recognizes it wherever it recognizes an ordinary
\code{\link[stats:family]{stats::family}} object.
}
\description{
\code{custom_family()} takes the log density itself, as an R function, and returns
a family object \code{\link[=bartisan]{bartisan()}} fits the model for. It is the route to a
response distribution none of the compiled families in
\code{\link{bartisan-families}} cover.
}
\details{
Nothing else about the sampler changes: the leaf-level Laplace proposal needs
the first two derivatives of the log density with respect to each additive
predictor and nothing more, and central differences of the supplied function
produce both.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# A Poisson model written out by hand. Terms free of eta may be dropped;
# they cancel from every acceptance ratio.
pois <- custom_family(function(y, eta) y * eta[, 1] - exp(eta[, 1]),
                      start = log(mean(d$y)))

# Two predictors: a mean and a log standard deviation.
ls <- custom_family(function(y, eta) dnorm(y, eta[, 1], exp(eta[, 2]),
                                           log = TRUE),
                    num_predictors = 2, start = c(0, 0))
}\if{html}{\out{</div>}}

The function is called once per leaf per Fisher-scoring step with the
observations reaching that leaf, so it must be vectorized over \code{y} and the
rows of \code{eta}; it must not be vectorized \emph{within} an observation, and it must
return exactly one value per row. Supplying \code{derivatives} cuts three calls to
one and removes the differencing error.

Because it sees a subset rather than the whole sample, anything else the
density needs has to be a scalar or reach it through \code{eta}. A
per-observation vector captured from the enclosing environment will not line
up with the rows it is handed, and nothing can detect that for you: the
fit runs and is wrong. Where such a quantity is genuinely needed, an offset
belongs in the formula and a varying trial count or exposure belongs in a
family written for it.
\subsection{Nuisance Parameters}{

These are drawn alongside the trees when \code{aux_names} names them, and
\code{logdens} then takes a third argument holding their current values:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# A Gaussian written out by hand, with its scale drawn rather than fixed.
by_hand <- custom_family(
  logdens = function(y, eta, aux) dnorm(y, eta[, 1], exp(aux[1]), log = TRUE),
  aux_names = "log_sigma", aux_start = 0)
}\if{html}{\out{</div>}}

They are reported in \code{fit$aux} under those names, and covered by \code{summary()}
and \code{\link[=diagnose]{diagnose()}} like any other family's. There is no prior argument and no
bounds argument, because a nuisance parameter here is carried as an additive
predictor whose forest is pinned at depth zero (one tree that can never
split, so the forest is a single scalar), and it is drawn by the same
Laplace-plus-Metropolis step as any leaf, under that step's Gaussian leaf
prior. So a parameter with a restricted range is handled the way it would be
for a real predictor, by writing the transform into \code{logdens}: the \code{exp()}
above is what keeps the scale positive.
}

\subsection{Limits of a Log Density}{

A density says how likely an observed value is, not how to draw a new one, so
a \code{custom_family()} fit has no posterior predictive distribution, which is
what \code{\link[=bartisan-interop]{simulate()}}, \code{\link[=bartisan-interop]{pp_check()}} and
\code{\link[=bartisan-interop]{r2()}} rely on. Each of them errors on such a fit
rather than returning something it cannot support. \link[=bartisan-interop]{loo()}
and \code{\link[=bartisan-interop]{waic()}} are unaffected, since both read the
pointwise log likelihood the family already computes.

There are smaller limits as well. The response must be numeric, so a factor
has to be coded
first. And since the package cannot know what the mean of the density is,
\code{predict(type = "response")} returns the additive predictors rather than a
fitted mean.
}
}
\examples{
set.seed(123)

d <- data.frame(x1 = runif(300), x2 = runif(300))
d$y <- rpois(300, exp(1 + sin(pi * d$x1)))

# A Poisson likelihood written out by hand, with its derivatives. Terms
# free of eta may be dropped, since they cancel from every acceptance
# ratio, and the same terms are absent from the score.
pois <- custom_family(
  function(y, eta) y * eta[, 1] - exp(eta[, 1]),
  derivatives = function(y, eta, h) {
    list(score = y - exp(eta[, 1]), info = exp(eta[, 1]))
  },
  start = log(mean(d$y)))

fit <- bartisan(y ~ x1 + x2, data = d, family = pois,
                num_trees = 20, num_burn = 100, num_draws = 100,
                verbose = FALSE)

fit

# A beta-binomial, which no built-in family covers: counts out of a known
# number of trials, overdispersed relative to a binomial. `phi` is drawn
# alongside the trees, and `size` is a scalar, so closing over it is safe.
size <- 30
d$hits <- rbinom(300, size, rbeta(300, plogis(d$x1) * 6,
                                  (1 - plogis(d$x1)) * 6))

bb <- custom_family(
  logdens = function(y, eta, aux) {
    p <- plogis(eta[, 1])
    phi <- exp(aux[1])
    lbeta(y + p * phi, size - y + (1 - p) * phi) -
      lbeta(p * phi, (1 - p) * phi)
  },
  aux_names = "log_phi", aux_start = log(5), name = "beta-binomial")

fit_bb <- bartisan(hits ~ x1 + x2, data = d, family = bb,
                   num_trees = 20, num_burn = 100, num_draws = 100,
                   verbose = FALSE)

# The drawn precision, on the scale it was written on.
exp(mean(fit_bb$aux[, "log_phi"]))

}
\seealso{
\code{\link{bartisan-families}} for the compiled families, one of which is usually the
better answer; \code{\link[=bartisan]{bartisan()}} for fitting a model with the result;
\code{vignette("families", package = "bartisan")} for the long form
}
