% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.bartisan_fit}
\alias{predict.bartisan_fit}
\title{Predictions from a generalized BART model}
\usage{
\method{predict}{bartisan_fit}(
  object,
  newdata = NULL,
  type = "response",
  draws = FALSE,
  iterations = NULL,
  offset = NULL,
  weights = NULL,
  values = NULL,
  log = FALSE,
  times = NULL,
  ...
)
}
\arguments{
\item{object}{a \verb{<bartisan_fit>} object; the output of a call to \code{\link[=bartisan]{bartisan()}}.}

\item{newdata}{optional; a data frame at which to predict. Default is \code{NULL}
to use the data the model was fit to. Missing predictor values are allowed
in the columns that had them when the model was fit, since only those
columns' splitting rules carry an answer for one; a missing value anywhere
else is an error. See section \emph{Missing Predictor Values} in the Details of
\code{\link[=bartisan]{bartisan()}}.}

\item{type}{string; the scale of the prediction. Allowable options include
\code{"link"}, \code{"response"} (the default), \code{"prob"}, \code{"class"}, \code{"mean"},
\code{"stdlv"}, \code{"density"}, and \code{"survival"}.
\describe{
\item{\code{"link"}}{the additive predictor, one column per predictor for
families that have more than one.}
\item{\code{"response"}}{the mean of the response; the median survival time for
every survival family, \code{\link[=ph]{ph()}} included; and, for a response with
categories, the category probabilities, there being no single mean to
report.}
\item{\code{"prob"}}{category probabilities, for the binomial, ordinal and
multinomial families.}
\item{\code{"class"}}{the most probable category, as a factor, for the same
families.}
\item{\code{"mean"}}{the mean of the response with the category labels read as
numbers, for the same families, so that \code{"4"} counts as four. \code{values}
says what the categories are worth where the labels are not the numbers
intended.}
\item{\code{"stdlv"}}{the additive predictor on the scale of the latent
variable it indexes, for the ordinal and binomial families under the
probit, logit and complementary log-log links. This puts fits with
different links, or different amounts of signal, on one scale. See
Details.}
\item{\code{"density"}}{the conditional density of the outcome given the
predictors, evaluated at the observed outcome, so \code{newdata} must carry
the outcome and leaving it empty uses the fitted data. This is the
observation's likelihood contribution, so it is a probability for a
discrete response and a survival probability for a censored time. See
Details.}
\item{\code{"survival"}}{the survival function \eqn{S(t \mid x)} at the times
given in \code{times}, for the accelerated failure time families and \code{\link[=ph]{ph()}}.
Returns one column per time, or a draws by rows by times array when
\code{draws = TRUE}. This is what makes the usual survival estimand reachable
through \pkg{marginaleffects}; see \code{\link{bartisan-marginaleffects}}.}
}}

\item{draws}{\code{logical}; whether to return every posterior draw rather than
the posterior mean. Default is \code{FALSE} to return the mean. If \code{TRUE}, the
result gains a leading dimension indexing the draws.}

\item{iterations}{\code{numeric}; optional indices of the stored draws to use,
between 1 and the number of draws the fit retains. Default is \code{NULL} to use
all of them.}

\item{offset}{\code{numeric}; an offset for the rows of \code{newdata}, on the link
scale, given either as one value per row or as a matrix with one column per
additive predictor. Used only when \code{newdata} is supplied, and required there
when the model was fit with an observation-level offset, since an offset is
not a function of the predictors and so cannot be reconstructed. Default is
\code{NULL} to add nothing.}

\item{weights}{\code{numeric}; prior weights for the rows of \code{newdata}, used only
by \code{type = "density"} and ignored otherwise. For a binomial response given
as proportions these are the numbers of trials, as in \code{\link[stats:glm]{stats::glm()}}.
Default is \code{NULL}, which uses the weights the model was fit with when
\code{newdata} is omitted and a weight of 1 for every row when it is supplied.}

\item{values}{\code{numeric}; for \code{type = "mean"}, what each response category is
worth, given as a vector named for every level of the response. Default is
\code{NULL} to read the level labels as numbers, which is an error rather than a
guess when they cannot be read that way. Ignored with a warning for every
other \code{type}.}

\item{log}{\code{logical}; for \code{type = "density"}, whether to return the log of the
value. Default is \code{FALSE}. Summing the log density across observations gives
a log score. Note that with \code{draws = FALSE} the density is averaged over the
draws before the log is taken, so the result is the pointwise predictive
density rather than the average log density.}

\item{times}{\code{numeric}; for \code{type = "survival"}, the times at which to report
the survival function, which must be finite and strictly positive. It has no
default, because the horizon is a choice rather than a property of the fit.
Ignored with a warning for every other \code{type}.}

\item{...}{ignored; present for compatibility with the generic.}
}
\value{
With \code{draws = FALSE}, a vector with one element per observation for
\code{"response"}, \code{"mean"} and \code{"density"}, and for \code{"link"} and \code{"stdlv"} when
the family has a single additive predictor; a matrix of observations by
additive predictors for \code{"link"} and \code{"stdlv"} when it has more than one; a
matrix of observations by categories for \code{"prob"}, and for \code{"response"} with
an ordinal or multinomial family; a matrix of observations by times for
\code{"survival"}; and a factor for \code{"class"}, ordered when the family is
\code{ordinal()}.

With \code{draws = TRUE} each of these gains a leading dimension indexing the
posterior draws, so that a vector becomes a matrix of draws by observations
and a matrix becomes an array of draws by observations by categories or by
times. On the \code{"link"} and \code{"stdlv"} scales a family with more than one
additive predictor returns a list with one draws-by-observations matrix per
predictor. \code{"class"} is a factor either way.
}
\description{
Computes predictions from a \code{\link[=bartisan]{bartisan()}} fit, at the data the model was fit
to or at new data, on any of several scales. Because every posterior draw of
every tree is retained, a prediction can be returned as its posterior mean or
as the draws themselves.
}
\details{
\subsection{Simulated Multinomial Probit Probabilities}{

The likelihood of a \code{multinomial("probit")} fit has no closed form: the
probability of a category is the chance that the largest of several correlated
Gaussian variables is the one belonging to it, which is a multivariate orthant
probability. So \code{"prob"}, \code{"class"}, \code{"response"} and \code{"density"} are all
simulated, using the number of replicates the family was given. Fresh draws
are taken on every call, so two calls differ by Monte Carlo error; that error
is per posterior draw and averages down over them, which makes \code{draws = FALSE}
much more accurate than any single row of \code{draws = TRUE}.
}

\subsection{Setting \code{type = "stdlv"}}{

This reports \verb{(eta - E[e]) / sd(y*)} for the latent \verb{y* = eta + e}, following
\pkgfun{WeightIt}{predict.ordinal_weightit}, and is what makes an ordinal or
binary fit comparable across links.

The scale is \verb{sd(y*) = sqrt(var(eta) + var(e))}, where \code{var(eta)} is taken
over the sample the model was fitted to, per draw, so it is a property of the
model rather than of whatever is being predicted and the same divisor is used
for new data. \code{var(e)} is whatever the link implies: 1 for the probit link,
\eqn{\pi^2 / 3} for the logit, \eqn{\pi^2 / 6} for the complementary
log-log. The location subtracts the latent error's mean, which is invisible
for the logit and probit links, whose errors are already centered, and is the
whole of the difference for the complementary log-log.

Such a model is identified only up to a common shift of its thresholds and its
predictor, so the location here is a convention, and the one used is the same
the cutpoints use: a predictor centered over the fitted sample. Differences on
this scale, which is what a standardized quantity is for, are unaffected by
that choice.
}

\subsection{Setting \code{type = "density"}}{

The measure differs across the survival families: the accelerated failure time
families, \code{dpm_aft()} included, report the density of \eqn{\log T}, where
\code{\link[=ph]{ph()}} reports the density of \eqn{T}. The two differ by \eqn{\sum \log t},
so log scores are comparable within each group rather than across them, and
\code{type = "survival"} is comparable throughout. \code{\link[=bartisan-interop]{loo()}} takes a
\code{scale} argument that puts them on one measure.

\code{type = "density"} returns \code{NaN} for an observation whose density is undefined
at any of the draws, which happens when a link the package does not compile
has been composed onto the family's own scale and its inverse does not cover
the whole additive predictor (e.g., \code{poisson("identity")}, which gives a
positive mean only where the predictor is positive). A warning reports how
many draw-by-observation values were undefined and how many returned values
that made \code{NaN}. The draws are averaged before the log is taken, so one
undefined draw is enough to make an observation \code{NaN}.
}
}
\examples{
data("rhc")
set.seed(123)

# Whether a patient died, with every other variable a candidate predictor
fit <- bartisan(death ~ . - days, data = rhc,
                num_trees = 10, num_burn = 50, num_draws = 50)

# Fitted probabilities of death, averaged over the draws
head(predict(fit, type = "response"))

# The whole posterior for the first five patients rather than its mean
post <- predict(fit, newdata = rhc[1:5, ], draws = TRUE)
apply(post, 2, quantile, c(.025, .5, .975))

# A held-out log score, which needs the outcome, so `newdata` carries it
sum(log(predict(fit, newdata = rhc[1:100, ], type = "density")))

}
\seealso{
\code{\link[=bartisan]{bartisan()}} for fitting the model; \code{\link{bartisan-marginaleffects}} for averages
and contrasts of these predictions; \code{\link{bartisan-interop}} for the methods that
let other packages assess the fit
}
